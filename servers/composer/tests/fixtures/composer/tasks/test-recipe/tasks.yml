---
pipeline: test-pipeline

variables:
  global_var: "Global"
  target_schema: "fake_schema"

assets:
  task_asset:
    path: "{{project_path}}/task-asset.json"
    writeable: false

steps:
  header:
    template: "Global instructions.\nSchema: {{target_schema}}"

  step1:
    template: "Step 1: {{step1_instructions}}"
  
  step2:
    template: "Step 2: {{step2_instructions}}"

