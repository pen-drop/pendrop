---
variables:
  pipeline_var: "pipeline_value"

assets:
  pipeline_asset:
    path: "{{project_path}}/pipeline-asset.json"
    writeable: false

steps:
  header:
    description: "Header step"
  step1:
    description: "Step 1"
    dependencies: ["header"]
  step2:
    description: "Step 2"
    dependencies: ["step1"]
